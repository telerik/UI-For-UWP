<local:ExamplePageBase
    x:Class="SDKExamples.UWP.DatePicker.PickerButtonCustomizations"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SDKExamples.UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:input="using:Telerik.UI.Xaml.Controls.Input"
    xmlns:dateTimePicker="using:Telerik.UI.Xaml.Controls.Input.DateTimePickers"
    mc:Ignorable="d" x:Name="page">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding Title, ElementName=page}" Style="{StaticResource ExampleHeaderTextBlockStyle}" />
        <input:RadTimePicker Grid.Row="1" DisplayValueFormat="mm:h" Background="Beige" FontSize="21" FontStyle="Italic" Margin="10"/>
    </Grid>
</local:ExamplePageBase>
